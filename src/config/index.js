// const allowingServer = ["http://localhost:4000", "http://localhost:8000"];

// const corsOptions = {
//   origin: (origin, callback) => {
//     if (!origin || allowingServer.includes(origin)) {
//       callback(null, true);
//     } else {
//       callback(new Error("Not allowed by CORS"));
//     }
//   },
//   credentials: true,
//   optionsSuccessStatus: 200,
// //   exposedHeaders: ["WWW-Authenticate"],
// };

// export { corsOptions };